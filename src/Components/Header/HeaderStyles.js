import { AppBar } from "@mui/material";
import styled from "@emotion/styled";
export const HeaderStyle = styled(AppBar)(() => ({
  height: "72px",
  backgroundColor: "#FFFFFF",
}));
export const HeaderAddItemButton = {
  backgroundColor: "#2DCA73",
  height: "40px",
  width: "107px",
  color: "#FFFFFF",
  fontSize: "14px",
};
